<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>{% block title %}Commander un serivce{% endblock %}</title>

    {% block stylesheets %}
      {# On charge le CSS de bootstrap depuis le site directement #}
      <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
    {% endblock %}
    <style media="screen">
      body{
        background-color: #363940;
      }
      .margined{
        margin-top: 10%;
      }
    </style>
  </head>
<body>
  <div class="container margined">
    <div class="col-sm-1"></div>
    <div class="well col-sm-10">
<div class="container-fluid" style="text-align: center;">
      <h2 style="text-decoration: underline; font-weight: bold; margin-bottom: 5%">Récapitulatif de la commande</h2>
<pre style="text-align: left;">   Nom d'utilisateur : {{ commande.nom }}</pre>
<pre style="text-align: left;">   Adresse email : {{ commande.email }}</pre>
<pre style="text-align: left;">   Service souhaité : {{ commande.titre }}</pre>
<pre style="text-align: left;">   Description de votre projet : {{ commande.description }}</pre>
<br>
<a href="{{ path('portfolio_index') }}"><h4>Retourner sur le site</h4></a>
</div>
</html>
